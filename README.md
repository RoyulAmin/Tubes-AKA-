# Pyramid Solitaire (DFS Solver)

Folder structure:
- game/: engine (deck, pyramid board, moves)
- solver/: DFS recursive search
- gui/: Tkinter GUI
- run_game.py: start the app

Requirements:
- Python 3.8+
- tkinter (usually included with Python)

Run:
```bash
python run_game.py
